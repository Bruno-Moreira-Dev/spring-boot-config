# PROD environment

app:
  message: This is property file to the ${spring.application.name} application for ${spring.profiles.active} environment

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: <MYSQL_URL>
    username: <USERNAME>
    password: <PASSWORD>
